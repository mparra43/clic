
import { Transversal } from '@/components/transversal/sections';


interface Props {
  params: {
    id: string;
  };
}

export default async function Page({ params }: Props) {
  
  const { id } = params;

 

  
  return (
    <main className='flex min-h-screen justify-center lg:pt-16 pb-12 lg:pb-24 bg-gray-50 lg:bg-gray-100'>
      
    </main>
  )
} 
